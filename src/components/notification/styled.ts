import styled from 'styled-components';

export const RootDiv = styled.div`
  position: absolute;
  display: flex;
  flex-direction: column;
  width: 100px;
  background-color: #f00;
  height: 80px;
  right: 0;
  bottom: 0;
`;

export const EventMessageDiv = styled.div`
  color: #000;
`;

export const CloseButton = styled.div``;
