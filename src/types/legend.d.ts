export interface userHeartLegendType {
  no: string;
  sender: {
    no: number;
    nickname: string;
    image: string;
  };
  receiver: {
    no: number;
    nickname: string;
    image: string;
  };
}
