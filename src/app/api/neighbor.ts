// import { AxiosResponse } from "axios";

// const NEIGHBOR = {async setPresentStatus(no: number): Promise<any> {
//     const result: AxiosResponse = await instance.p(
//       `${MAILBOX.path}/presents/${no}`,
//     );
//     return result.data;};
