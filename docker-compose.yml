services:
  front:
    image: cyoure/frontdocker:tagname
    ports:
      - '3000:3000'
    env_file:
      - ./.env
    restart: always

  nginx:
    image: nginx:latest
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./conf/nginx.conf:/etc/nginx/nginx.conf
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/cetbot
    restart: always

  certbot:
    image: certbot/certbot
    depends_on:
      - nginx

    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot

    entrypoint: "/bin/sh -c 'trap exit TERM; if [ ! -e /etc/letsencrypt/live/testmodernworld.shop/fullchain.pem ]; then certbot certonly --webroot --webroot-path=/var/www/certbot -d testmodernworld.shop -d www.testmodernworld.shop --email w9559@naver.com --agree-tos --no-eff-email; fi; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
